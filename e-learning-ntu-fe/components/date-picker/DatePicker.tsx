import type { DatePickerProps } from './types';
export function DatePicker({ label, error, id, className = '', ...props }: DatePickerProps) { const inputId = id ?? 'date-picker'; return <label className="grid gap-2 text-sm font-medium" htmlFor={inputId}>{label}<input id={inputId} type="date" className={`rounded-lg border bg-white px-3 py-2 outline-none focus:ring-2 focus:ring-indigo-500 dark:border-slate-700 dark:bg-slate-950 ${error ? 'border-rose-500' : 'border-slate-300'} ${className}`} {...props} />{error && <span className="text-xs text-rose-600">{error}</span>}</label>; }
