import { FileCard } from '../file-card'; import type { FileListProps } from './types';
export function FileList({ files, onRemove }: FileListProps) { return <div className="grid gap-3">{files.length ? files.map((file) => <FileCard key={file.id} {...file} onRemove={onRemove ? () => onRemove(file.id) : undefined} />) : <p className="rounded-lg border border-dashed border-slate-300 p-4 text-sm text-slate-500 dark:border-slate-700">Chưa có tệp nào.</p>}</div>; }
