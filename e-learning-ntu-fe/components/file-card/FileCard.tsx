import type { FileCardProps } from './types';
export function FileCard({ name, size, type = 'FILE', onRemove }: FileCardProps) { return <div className="flex items-center gap-3 rounded-xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-900"><span className="grid h-10 w-10 place-items-center rounded-lg bg-indigo-100 text-xs font-bold text-indigo-700 dark:bg-indigo-950 dark:text-indigo-200">{type.slice(0, 4)}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{name}</p>{size && <p className="text-xs text-slate-500">{size}</p>}</div>{onRemove && <button onClick={onRemove} className="rounded p-1 text-slate-500 hover:text-rose-600" aria-label={`Xóa ${name}`}>×</button>}</div>; }
