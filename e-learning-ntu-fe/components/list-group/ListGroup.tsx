import Link from 'next/link'; import type { ListGroupProps } from './types';
export function ListGroup({ items }: ListGroupProps) { return <ul className="overflow-hidden rounded-xl border border-slate-200 divide-y divide-slate-100 dark:border-slate-800 dark:divide-slate-800">{items.map((item) => <li key={item.title}>{item.href ? <Link href={item.href} className="block bg-white p-4 hover:bg-slate-50 dark:bg-slate-900 dark:hover:bg-slate-800"><Item {...item} /></Link> : <div className="bg-white p-4 dark:bg-slate-900"><Item {...item} /></div>}</li>)}</ul>; }
function Item({ title, description, meta }: ListGroupProps['items'][number]) { return <div className="flex justify-between gap-4"><div><p className="font-semibold">{title}</p>{description && <p className="mt-1 text-sm text-slate-500">{description}</p>}</div>{meta && <span className="text-sm text-slate-500">{meta}</span>}</div>; }
