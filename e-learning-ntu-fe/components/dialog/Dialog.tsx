'use client';
import { useEffect } from 'react'; import type { DialogProps } from './types';
export function Dialog({ open, onClose, title, children, footer }: DialogProps) { useEffect(() => { const close = (e: KeyboardEvent) => e.key === 'Escape' && onClose(); window.addEventListener('keydown', close); return () => window.removeEventListener('keydown', close); }, [onClose]); if (!open) return null; return <div className="fixed inset-0 z-40 grid place-items-center bg-slate-950/60 p-4" role="presentation" onMouseDown={onClose}><section role="dialog" aria-modal="true" aria-labelledby="dialog-title" className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl dark:bg-slate-900" onMouseDown={(e) => e.stopPropagation()}><header className="flex items-start justify-between gap-4"><h2 id="dialog-title" className="text-lg font-bold">{title}</h2><button onClick={onClose} aria-label="Đóng" className="text-2xl text-slate-500">×</button></header><div className="py-5 text-sm text-slate-600 dark:text-slate-300">{children}</div>{footer && <footer className="flex justify-end gap-3">{footer}</footer>}</section></div>; }
