import type { TextareaProps } from './types';
export function Textarea({ label, error, id, className = '', ...props }: TextareaProps) { const inputId = id ?? `textarea-${label?.replace(/\s/g, '-').toLowerCase()}`; return <label className="grid gap-2 text-sm font-medium" htmlFor={inputId}>{label}<textarea id={inputId} className={`min-h-28 rounded-lg border bg-white px-3 py-2 outline-none ring-indigo-500 focus:ring-2 dark:bg-slate-950 ${error ? 'border-rose-500' : 'border-slate-300 dark:border-slate-700'} ${className}`} {...props} />{error && <span className="text-xs text-rose-600">{error}</span>}</label>; }
