'use client';
import type { AlertItem, AlertVariant } from './types';
const theme: Record<AlertVariant, { wrap: string; icon: string }> = {
  info: { wrap: 'border-sky-200 bg-sky-50 text-sky-950 dark:border-sky-900 dark:bg-sky-950/60 dark:text-sky-100', icon: 'bg-sky-500' }, success: { wrap: 'border-emerald-200 bg-emerald-50 text-emerald-950 dark:border-emerald-900 dark:bg-emerald-950/60 dark:text-emerald-100', icon: 'bg-emerald-500' }, error: { wrap: 'border-rose-200 bg-rose-50 text-rose-950 dark:border-rose-900 dark:bg-rose-950/60 dark:text-rose-100', icon: 'bg-rose-500' }, warning: { wrap: 'border-amber-200 bg-amber-50 text-amber-950 dark:border-amber-900 dark:bg-amber-950/60 dark:text-amber-100', icon: 'bg-amber-500' },
};
export function Alert({ item, onDismiss }: { item: AlertItem; onDismiss?: () => void }) { const symbol = item.variant === 'success' ? '✓' : item.variant === 'info' ? 'i' : '!'; return <div role="alert" className={`flex gap-3 rounded-xl border p-4 shadow-sm ${theme[item.variant].wrap}`}><span className={`grid h-6 w-6 shrink-0 place-items-center rounded-full text-sm font-bold text-white ${theme[item.variant].icon}`}>{symbol}</span><div className="min-w-0 flex-1"><p className="font-semibold">{item.title}</p>{item.message && <p className="mt-1 text-sm opacity-80">{item.message}</p>}</div>{onDismiss && <button onClick={onDismiss} className="rounded p-1 text-lg leading-none opacity-60 hover:opacity-100" aria-label="Đóng thông báo">×</button>}</div>; }
