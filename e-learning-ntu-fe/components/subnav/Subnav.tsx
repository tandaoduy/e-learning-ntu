import Link from 'next/link'; import type { SubnavProps } from './types';
export function Subnav({ items }: SubnavProps) { return <nav className="overflow-x-auto border-b border-slate-200 dark:border-slate-800" aria-label="Secondary navigation"><ul className="flex min-w-max gap-5">{items.map((item) => <li key={item.href}><Link href={item.href} className={`block border-b-2 px-1 py-3 text-sm font-semibold ${item.active ? 'border-indigo-600 text-indigo-600 dark:text-indigo-300' : 'border-transparent text-slate-500 hover:text-slate-800 dark:hover:text-white'}`}>{item.label}</Link></li>)}</ul></nav>; }
